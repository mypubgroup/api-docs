{
  "name": "api-docs",
  "private": true,
  "description": "Static API reference site for the MyPubGroup API, built from openapi.json and published to GitHub Pages.",
  "scripts": {
    "build": "redocly build-docs openapi.json --output dist/index.html --title \"MyPubGroup API\""
  },
  "devDependencies": {
    "@redocly/cli": "^2.51.2"
  }
}
